export default Slider
declare function Slider({
  children,
  onSlideComplete,
  onSlideStart,
  activeIndex,
  threshHold,
  transition,
  scaleOnDrag,
}: {
  children: Node
  onSlideComplete: Function
  onSlideStart: Function
  activeIndex?: Number
  threshHold?: number
  transition?: number
  scaleOnDrag?: boolean
}): ReactElement
declare namespace Slider {
  namespace propTypes {
    const children: Node
    const onSlideComplete: Function
    const onSlideStart: Function
    const activeIndex: Number
    const threshHold: number
    const transition: number
    const scaleOnDrag: boolean
  }
}
